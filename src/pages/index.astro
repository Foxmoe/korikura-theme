---
// index page

import Default from "@/layouts/default.astro";
import Button from "@/components/button.astro";

import { getCollection } from "astro:content";

const posts = await getCollection("posts");

console.log(posts);
---

<Default title="首页">
  <div style="text-align: center; padding: 3rem 0;">
    <div
      style="font-size: 3rem; display: inline-block; animation: float 3s ease-in-out infinite;"
    >
      =w=
    </div>
    <h1 style="font-size: 3rem; color: #d4888f; margin: 0.5rem 0;">
      Baka小狐 の 日记
    </h1>
    <p style="color: #b88a8a; font-size: 1.2rem;">才不是笨蛋呢!</p>
    <p style="color: #c9a8a8; margin-top: 2rem;">✨ 正在建设中 ✨</p>
  </div>
  
</Default>

<style is:global>
  @keyframes float {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-8px);
    }
  }
</style>
